'use strict';

var config = {
  build: '.build',
  buildPort: 8010,
  dist: 'dist',
  distPort: 8012,
  example: 'example',
  examplePort: 8013,
  livereloadPort: 8019,
  src: 'src',
  templatePort: 8014,
  test: 'test',
  testPort: 8011
};

module.exports = config;